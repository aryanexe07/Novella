- [ ] Fix @import order in src/app/globals.css (move Google fonts import after any @theme / @layer restrictions as required by the CSS processor).
- [ ] Verify build/lint passes (run next lint / build).

